{
  "name": "@tabnas/abnf",
  "version": "0.4.7",
  "main": "dist/abnf.js",
  "type": "commonjs",
  "types": "dist/abnf.d.ts",
  "description": "ABNF grammar compiler for the tabnas parser.",
  "homepage": "https://github.com/tabnas/abnf",
  "keywords": [
    "tabnas",
    "abnf",
    "grammar",
    "parser",
    "plugin"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/tabnas/abnf.git",
    "directory": "ts"
  },
  "scripts": {
    "pretest": "sh ../test/fetch-abnf-corpus.sh",
    "test": "npm run test-unit && npm run test-conformance",
    "test-unit": "node --enable-source-maps --test --test-skip-pattern='^conformance' test/**/*.test.js",
    "test-conformance": "node --enable-source-maps --test test/conformance.test.js",
    "watch": "tsc --build src -w",
    "build": "tsc --build src",
    "clean": "rm -rf node_modules dist dist-test yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: ts/v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag ts/v$REPO_VERSION && git push origin ts/v$REPO_VERSION;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag",
    "repo-stage": "npm run clean && npm i && npm run repo-stage-quick",
    "repo-stage-quick": "npm run build && npm run test && npm stage publish",
    "repo-stage-list": "npm stage list",
    "repo-stage-approve": "npm stage approve"
  },
  "license": "MIT",
  "files": [
    "dist",
    "bin",
    "LICENSE"
  ],
  "bin": {
    "tabnas-abnf": "bin/tabnas-abnf"
  },
  "exports": {
    ".": "./dist/abnf.js"
  },
  "peerDependencies": {
    "@tabnas/parser": ">=0.5.0",
    "@tabnas/bnf": ">=0"
  },
  "devDependencies": {
    "@tabnas/debug": "*",
    "@tabnas/parser": "*",
    "@tabnas/railroad": "*",
    "@tabnas/support": "*",
    "@types/node": "25.6.0",
    "typescript": "6.0.3",
    "@tabnas/bnf": "*"
  },
  "engines": {
    "node": ">=24"
  }
}
